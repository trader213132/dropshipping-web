# dropshipping web

## gstack

gstack is installed at `~/.claude/skills/gstack`.

Skills are invoked with `/` — e.g. `/ship`, `/qa`, `/spec`, `/review`.

Do not enable team mode. Collaborators are not required to have gstack.
